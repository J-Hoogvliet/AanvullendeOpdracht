import { Actor, Input, Scene } from "excalibur";
import { Sea } from "./sea";

export class FishingRod extends Actor {
  constructor(x, y) {
    super(x, y);
    this.width = 20;
    this.height = 20;
  }

  
}